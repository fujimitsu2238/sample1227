let users = [
    {id: 1, name: "John"},
    {id: 2, name: "Pete"},
    {id: 3, name: "Mary"}
  ];
  
  let user = users.find(item => item.id == 1);
  
  console.log(user.name);

  let arry = [0,"0",()=>console.log("hello")]

  function arry(item) {
    return item.id ==1
    
  }

  let user2 = {id: 1, name:"john"}
